import csv


fi = open(finame, 'r')
    rdr = csv.reader(fi, delimiter=' ', skipinitialspace=True)
    freq = 100.
    SWR = 999.
    table = []
    theta=0
    phi=0
    for row in rdr:
        print(row)
        if( row[0] == 'freq'):
            freq = float(row[2])
        elif( row[0] == 'SWR'):
            
            SWR = float(row[2])
        elif( row[0] == 'Theta' ):
            continue
        else:
            theta2 = float(row[0])
